faster-whisper>=1.1.0
sounddevice>=0.4.6
numpy>=1.26
pynput>=1.7.6
uiautomation>=2.0.18
pydantic>=2.7
rapidfuzz>=3.9

# The control panel (--gui). Draws its window with the WebView2 runtime
# that already ships with Windows 10 and 11, so this installs 2.6 MB and
# bundles no browser. Not needed to run headless.
pywebview>=5.0

# CUDA runtime for ctranslate2. faster-whisper loads the model on the GPU
# without these and then dies on the first transcription with
# "Library cublas64_12.dll is not found" - they are not optional on Windows
# unless you have a system-wide CUDA toolkit. Harmless on a CPU-only box.
nvidia-cublas-cu12; platform_system == "Windows"
nvidia-cudnn-cu12; platform_system == "Windows"

# Only needed for [intent] backend = "claude". The default offline
# backend does not use it.
anthropic>=1.0.0
