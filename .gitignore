# local configuration (may contain machine-specific paths)
config.toml

# logs and captures
*.log
probe-*.txt

# python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# editors / os
.vscode/
.idea/
Thumbs.db
desktop.ini

# uiautomation drops this next to whatever imported it, usually when COM
# was not initialised on the calling thread - which the test suite does
# every run. It is a symptom, not a source file.
@AutomationLog.txt
